---
interface Props {
  activePage?: "home" | "tutoring" | "sat" | "contact";
  showAnnouncement?: boolean;
  ctaHref?: string;
  ctaLabel?: string;
}

const {
  activePage = "sat",
  showAnnouncement = true,
  ctaHref = "#schedule",
  ctaLabel = "View dates",
} = Astro.props;

const cohortAnnouncement = {
  label: "August SAT cohort",
  text: "August 18–September 10 · Live online",
  href: "/sat-math-bootcamp#schedule",
  linkLabel: "View the program",
};

const navigation = [
  { key: "home", label: "Home", href: "/" },
  {
    key: "tutoring",
    label: "Math Tutoring",
    href: "/tutoring",
  },
  { key: "sat", label: "SAT Math Bootcamp", href: "/sat-math-bootcamp" },
  {
    key: "contact",
    label: "Contact Me",
    href: "/contact",
  },
];
---

<div class="site-header-shell">
  {
    showAnnouncement && (
      <div class="announcement-bar">
        <div class="site-container announcement-inner">
          <span class="announcement-label">{cohortAnnouncement.label}</span>
          <span>{cohortAnnouncement.text}</span>
          <a href={cohortAnnouncement.href}>{cohortAnnouncement.linkLabel}</a>
        </div>
      </div>
    )
  }

  <header class="site-header">
    <div class="site-container header-inner">
      <a class="brand-lockup" href="/" aria-label="PiPath Academy home">
        <span>PiPath Academy</span>
        <small>Dr. Orlando Ferrer</small>
      </a>

      <nav class="primary-nav" aria-label="Primary navigation">
        {
          navigation.map((item) => (
            <a href={item.href} aria-current={activePage === item.key ? "page" : undefined}>
              {item.label}
            </a>
          ))
        }
      </nav>

      <a class="button button-small button-primary header-cta" href={ctaHref}>{ctaLabel}</a>
    </div>
  </header>
</div>
